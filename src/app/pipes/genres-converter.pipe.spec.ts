import { GenresConverterPipe } from './genres-converter.pipe';

describe('GenresConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new GenresConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
